% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny_ast.R
\name{shiny_sample_settings}
\alias{shiny_sample_settings}
\title{Create sample settings data frame from an Access Sensor Technologies (AST)
UPAS v2 or UPAS v2.1 PLUS header data frame.}
\usage{
shiny_sample_settings(df_h, fract_units = FALSE)
}
\arguments{
\item{df_h}{A header data frame returned by the \link{read_ast_header} function
with the argument \code{update_names = TRUE}.}

\item{fract_units}{Boolean to specify if units should be fractional (L min^-1 vs L/min).}
}
\value{
A modified single row header data frame with select sample settings data.
}
\description{
\code{shiny_sample_settings} modifies an Access Sensor Technologies UPAS v2 or UPAS v2.1 PLUS
header data frame
for use with the Sample Settings tab in the online shinyAST data analysis app.
This function sets the proper data type for each variable,
appends a sample PASS/FAIL flag column,
and adds units to the applicable variable names.
}
\examples{
multiple_upas_headers <- list.files(path = "inst/extdata", pattern="^PS.*.txt$",
full.names = TRUE) \%>\%
    lapply(read_ast_header, update_names=TRUE) \%>\%
    dplyr::bind_rows()

sample_settings <- shiny_sample_settings(multiple_upas_headers, fract_units=TRUE)
}
